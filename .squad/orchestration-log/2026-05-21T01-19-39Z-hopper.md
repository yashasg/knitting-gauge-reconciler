---
Agent: Hopper (Tooling Dev)
Timestamp: 2026-05-21T01:19:39Z
Requestor: yashasg
Mode: background
---

## Why routed

Spawn directive for swift-metrics scoping (GitLab issue #9, parallel 8-agent batch). Hopper owns §3 tooling and build integration.

## Files authorized to read

- `app/build.sh`
- `app/app.xcodeproj/project.pbxproj`
- `docs/swift_coding_standards.md`
- `.squad/identity/now.md`

## Files produced

- `.squad/decisions/inbox/hopper-metrics-scope.md` — Build/packaging scope (SPM package integration via Xcode, exact version pin 2.11.0 with 2.6.x fallback, no root Package.swift). Gating mechanism (`KGR_METRICS_BACKEND` env var with noop/inmemory/debug-print options). build.sh changes (one pass-through block for `TEST_RUNNER_*` env var mapping, no new mode/flag). Warnings gate scope (applies to first-party targets only, SPM packages have own settings). CI/CD impact (5–15s SPM resolution with cache recommendation). Optional lint rules (single bootstrap site, centralised labels enum, no Metrics import in math layer).

## Outcome

**Completed.** Tooling/build integration scoped. SPM dependency (apple/swift-metrics@2.11.0) ready to integrate via Xcode GUI. build.sh amendment drafted (env-var pass-through). -warnings-as-errors gate honoured by construction (package-internal settings do not affect first-party targets). Three open decisions flagged for Tesla: build-time SPM fetch carve-out, DEBUG default strategy, version-pin policy.
