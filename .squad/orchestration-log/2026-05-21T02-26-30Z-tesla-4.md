# Tesla-4 — MetricKit Standards Amendments (V3-IMPL)

**Agent:** Tesla (Lead / Architect)
**Session:** metrickit-pivot-shipped
**Timestamp:** 2026-05-21T02:26:30Z
**Dispatch:** 2026-05-20T19:26:30-07:00 (from Coordinator)
**Status:** SHIPPED
**Deliverables:** docs/swift_coding_standards.md amendments (§2.2/§2.3/§2.12/§7), GitLab issue #9 comment (note 3370575474)

## Standards Amendments

**File:** docs/swift_coding_standards.md

### §2.2 — Determinism in the math layer

**Change:** Added enforcement sentence

> "The math layer (`GaugeMath.swift`) MUST NOT import `MetricKit`, `os.signpost`, `os`, or any analytics framework. Verdict classification for analytics signposts lives in `GaugeMathMetrics.swift`, called by the view layer after `GaugeMath.compute(...)` returns. This is enforced by `MetricKitSubscriberTests.AC-3` (static file scan) and `AC-4` (runtime recording double)."

**Rationale:** Post-MetricKit-pivot clarity. Math layer remains pure; signpost decisions are view-layer responsibility.

### §2.3 — No network / no analytics upload

**Change:** Rewrote carve-out paragraph

Removed stale text about "on-device-only counters acceptable but must not transmit". Replaced with:

> "User code MUST NOT open sockets, instantiate `URLSession` to telemetry endpoints, or link any third-party analytics SDK (Firebase, Amplitude, Mixpanel, Segment, GoogleAnalytics, Sentry, or equivalents). System frameworks (`MetricKit`) that conduct OS-mediated upload at Apple's schedule, with user opt-out under iOS Settings → Privacy & Security → Analytics & Improvements → Share With App Developers, are PERMITTED. Re-export of `MXMetricPayload` via a developer-owned HTTP endpoint requires a separate §2.3 amendment naming the endpoint URL and stating a retention policy; this is explicitly DEFERRED to V2 and is NOT in scope for V1."

**Rationale:** MetricKit's system-mediated egress is a carve-out to the "no upload" rule. Developer endpoint (optional, deferred to V2) requires named amendment with URL + retention policy. User code sockets/SDKs remain forbidden.

### §2.12 — Logging / release discipline

**Change:** Added paragraph below existing no-log-in-release rule

> "`MXMetricManagerSubscriber.didReceive(_:)` handlers that log payload contents (e.g., `jsonRepresentation()` via `print` or `os_log`) MUST be wrapped in `#if DEBUG`. In release builds, `didReceive(_:)` is a no-op — the data still flows to App Store Connect Analytics via Apple's auto-pipeline, but our process never emits the contents."

**Rationale:** MetricKit logging in subscriber should be debug-only. Release path is silent pass-through.

### §7 — Open questions → RESOLVED

**Change:** Replaced two open-question bullets

Before: "(1) MetricKit-as-analytics pending decision. (2) Localisation scope TBD."

After:

> "RESOLVED 2026-05-20. MetricKit consumption (via `MXMetricManagerSubscriber` and `MXSignpost(_:)`) is in scope. Re-export of payloads to a developer-owned endpoint is forbidden by default — see §2.3 carve-out. The current roster of `MXSignpost` names (9 total) is documented in `.squad/decisions.md`. Any new signpost name requires a Lead review and an addition to `decisions.md`."

**Note:** Localisation open question (was second §7 bullet) is now unhoused. Should be added as new §8 bullet in separate amendment when localisation work is scoped.

**Rationale:** MetricKit decision is closed post-user-directive (2026-05-20T18:50:53). Signpost roster is locked at 9 names per user directive (2026-05-20T19:26:30).

## GitLab Issue #9 Comment

**Posted:** Note 3370575474
**URL:** https://gitlab.com/yashasg/knitting-gauge-reconciler/-/work_items/9#note_3370575474
**Status:** Ready for user review; will be visible at issue submission time

**Content summary:**
- Scope correction: apple/swift-metrics → MetricKit (system framework, zero SPM deps, OS-mediated daily aggregation)
- Explains why swift-metrics has no production sink (it's a vocabulary only; production backend must be supplied at bootstrap)
- Documents the 9-signpost locked roster (compute, share.invoked, share.fallback, reset.tapped, verdict.improved, verdict.degraded, sheet.verdictHelp.opened, sheet.aboutHelp.opened, cast_on.driftBandShown)
- States privacy posture (PrivacyInfo.xcprivacy + ASC labels, no in-app disclosure card per user directive 2026-05-20T19:22:50)
- Lists deferred items (developer endpoint re-export → V2)
- Lists files landing in scope (MetricsSubscriber.swift, GaugeMathMetrics.swift, ContentView.swift, PrivacyInfo.xcprivacy, MetricKitSubscriberTests.swift, standards amendments)
- References §2.2/§2.3/§2.12/§7 amendments to swift_coding_standards.md
- References user directives (2026-05-20T18:50:53, 2026-05-20T19:22:50, 2026-05-20T19:26:30)

## Prior Note Handling

**Note ID 3370481646** (earlier stale comment on V2 swift-metrics scope): Marked SUPERSEDED via glab api PUT. Visible in issue history but marked as no-longer-current.

## Notes

- All three user directives (metrics pivot, privacy card, signpost roster) are captured in the GitLab comment for transparency.
- §7 localisation open question deferred to future amendment; not blocking V1 ship.
- Signpost roster is now documentation-locked; any future additions require Lead review + decisions.md amendment.
